#include "main.h"
#include <string.h>

/**
* _strlen - print out length of a string
* @s: char pointer of string
*
* Return: int;
*/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
